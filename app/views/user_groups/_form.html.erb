<%= form_for @user_group, :html => { :class => "form-horizontal user_group" } do |f| %>

  <% if @user_group.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@user_group.errors.count, "error") %> prohibited this user_group from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @user_group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>
  <div class="form-group">
    <%= f.label :description, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :description, :class => 'form-control' %>
    </div>
    <%=f.error_span(:description) %>
  </div>
  <div class="form-group">
    <%= f.label :deleted_at, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :deleted_at, :class => 'form-control' %>
    </div>
    <%=f.error_span(:deleted_at) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                user_groups_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
