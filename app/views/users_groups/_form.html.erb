<%= form_for @users_group, :html => { :class => "form-horizontal users_group" } do |f| %>

  <% if @users_group.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@users_group.errors.count, "error") %> prohibited this users_group from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @users_group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

<!--   <div class="form-group">
    <%= f.label :group_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :group_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:group_id) %>
  </div>
  <div class="form-group">
    <%= f.label :user_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :user_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:user_id) %>
  </div> -->

<!--   <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                group_users_groups_path, :class => 'btn btn-default' %>
    </div>
  </div> -->

<% end %>
